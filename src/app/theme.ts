import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#21B8F9',
            dark: '#103B66',
            light: '#FFFFFF'
        },
    }
});

export default theme;
